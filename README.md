# Threat Intelligence Platform

Defensive IOC storage and review platform with IP, domain, URL, hash and email indicators, confidence tracking, SQLite persistence, and a JSON API.

## Run
```bash
python -m venv .venv
pip install -r requirements.txt
python app.py
```
Open http://127.0.0.1:5000.